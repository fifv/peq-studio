import type { Channel } from '../types.ts';
export function parseAny(text: string): unknown;
export function toRewText(channel: Channel): string;
export function toJsonText(channel: Channel): string;
